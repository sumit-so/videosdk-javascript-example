/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.html',    // HTML files in the `src` directory
    './src/**/*.js',      // JavaScript files in the `src` directory
    './js/**/*.js',       // JavaScript files in the `js` directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
